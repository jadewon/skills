# list-scheduled / cancel-scheduled 서브커맨드에서만 사용 (예약 자체는 MCP 도구로 토큰 불필요).
# slack-edit-message 스킬과 동일 토큰 — 이미 ~/.config/slack-user-token/.env 에 저장해뒀다면 이 파일은 필요 없음.
#
# 발급: https://api.slack.com/apps → 앱 선택 → OAuth & Permissions → User Token Scopes: chat:write
SLACK_USER_TOKEN=
