{
  "name": "slack-scheduled-message",
  "description": "Schedule a Slack message to a channel, sent as the bot so it actually notifies you, carrying a `claude --resume` command for the current session, plus list/cancel scheduled messages. Requires the local slack MCP server (jadewon/mcps slack-mcp).",
  "version": "1.3.0"
}
